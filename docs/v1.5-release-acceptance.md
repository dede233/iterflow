# 迭程 IterFlow V1.5 正式发布重新验收

## 结论与基线

- 验收日期：2026-09-24（Asia/Shanghai）。
- 正式验收基线：`master` merge commit `8f7262d3ee5934866ed3c0e7e61b81c9af79ce8f`；GitHub/Gitee `master` 均指向此 SHA。个人中心修复 `31aa496d7d63aa008e3070b409bbba787c9ea191` 已合入。
- 旧稳定标签与明确代码回滚点：`v0.8.3` → `9bcdac1afe8499ca3c471c678b411eace60833a6`，未移动。
- 本轮验收资产分支：`release/v1.5.0-reacceptance`；自动化提交 `ed93c2c80af615e687e5ee7716e00fee0a01d816`。原 dirty 验收工作区保持原样。
- **Release Verdict：BLOCKED。不得创建 `v1.5.0`，不得宣布正式发布。**

## 新发现的发布阻塞项

| 级别 | 证据 | 不满足的验收要求 |
| --- | --- | --- |
| BLOCKER | `frontend/src/views/notification/NotificationCenterView.vue` 的卡片点击只调用 `markNotificationRead(id)` 并重载列表；该组件不使用 router，也没有任何业务详情链接。发布事务在 `backend/app/services/version_service.py` 创建的通知明确带有 `entity_type="FEEDBACK"`、`entity_id=feedback.id`，`NotificationOut` 也公开这两个字段。 | 原 V1.5 Release Acceptance 第十四项要求提交人从站内通知正确跳转到业务对象。当前点击发布通知不会进入 `/feedbacks/{id}`。 |

这不是旧版个人中心问题，也不是 `publish/check` 方法冲突。需求方已确认发布检查按现有 **POST** 契约验收，清单中的 GET 是笔误。不得在本验收分支顺手修改通知页面；需单独修复并独立终审，然后从新基线重跑完整验收。

## 已完成的合并后证据（不构成整体 PASS）

| 范围 | 本轮结果与证据 |
| --- | --- |
| Master CI | [GitHub Actions run 35950509881](https://github.com/dede233/iterflow/actions/runs/35950509881) 的目标 SHA 为 `8f7262d3ee5934866ed3c0e7e61b81c9af79ce8f`；`backend`、`frontend`、`docker_smoke`、`s3`、`browser_smoke`、`backup_restore` 六项均为 success。包含 fresh-volume、真实 S3、生产构建和备份恢复演练。 |
| 后端与真实 PostgreSQL | 最终在隔离 schema 注入 `ITERFLOW_TEST_POSTGRES_URL` 后运行严格警告门禁：**247 passed、1 skipped**；唯一 skipped 是本机未启用的真实 S3 集成，由同 SHA 的 Master CI `s3` job 覆盖。专项目录 `backend/tests/test_v1_e2e_postgres.py` 为 **4 passed、0 skipped**。 |
| 主业务链路 | 新增一次性 RA 前缀用户/角色与数据；经真实 API 登录、首次改密、Feedback 提交/受理/转 Requirement、负责人和优先级、Version 关联、合法状态迁移、POST publish/check、发布、Release SUCCESS、Requirement/Feedback ONLINE 均通过。数据库检查主关系与每版本唯一 Release。 |
| 事务、并发 | 重复转需求返回 409 且无半成品；两个客户端旧 Requirement revision 保存返回 409、胜者数据未被覆盖；受控发布审计故障后 Version/Requirement/Feedback/Release/Notification/Audit 均无部分成功；真实 PostgreSQL 双管理员并发撤销测试保持 1 名有效 SUPER_ADMIN。 |
| RBAC / DataScope | 全量后端权限矩阵、SELF/ALL、Requirement+Version 条件授权、文件范围与业务附件访问测试通过。普通 MEMBER 的真实浏览器登录、首次改密和 `/profile` 访问通过；`/auth/me` 不含 `sys.user.edit`。 |
| 文件 | 完整后端测试覆盖 standalone SELF/ALL、业务附件隔离、关联文件删除保护、MIME/签名/OOXML、路径型文件名与存储路径拒绝；真实 S3 往返由 Master CI `s3` job 成功证明。 |
| 通知 | 发布后提交人的未读通知通过 API 可见，`entity_type`/`entity_id` 正确，标记已读成功；**前端业务跳转失败，见 BLOCKER**。 |
| 审计 | 本轮真实 PostgreSQL 主链路测试验证创建、受理、转化、需求状态、版本关联/状态、发布、Release、用户/角色操作动作集合，以及 operator、request_id、IP、User-Agent 和代表性 before/after；审计列表查询与范围测试通过。 |
| 前端与浏览器 | Vitest **97 passed**。真实 Chromium **5 passed**，涵盖首次强制改密、常规改密与新 token `/auth/me`、退出后重新鉴权、普通 MEMBER、登录与个人中心五档视口，以及核心/管理页面的可见性和横向溢出检查。视口：375、390、768、1280、1440 px。个人中心长用户名/邮箱和六项移动导航不重叠。 |
| OpenAPI 与构建 | runtime/static OpenAPI parity、生成的 TypeScript DTO、前端 build 均通过；最大 JavaScript chunk **297.1 KiB**，bundle 检查通过。 |
| 质量与依赖安全 | Ruff、format、Mypy、compileall 通过；npm 完整及生产依赖审计 **0 vulnerabilities**，pip-audit **0 known vulnerabilities**。 |
| 数据库与 HTTP | 一次性 schema 从空库迁移至 `0004_integrity`，`alembic check`：`No new upgrade operations detected.`；无新增 migration。真实 HTTP：`/health` 200、`/ready` 200、Web `/` 200。验收用 schema、Redis DB 和临时上传目录均已清理。 |

## 未完成、风险及发布前执行项

- 因通知业务跳转 BLOCKER，正式验收按规则停止；不能把已通过的局部门禁汇总成整体 PASS。修复后仍须重新验证通知点击后的目标页面、权限/数据范围、已读状态，再完整复跑正式验收。
- 本机既有默认开发数据库仍可能停留在旧 revision；本轮没有迁移或改动它。上述 `0004_integrity` 与 Alembic 结论仅来自隔离验收 schema 及 Master CI 的全新部署。
- 本轮无本机 Docker 验收；Docker、S3、备份恢复引用合并后 Master CI 的成功证据，不伪称本机执行。
- Git 状态：测试自动化提交后，验收 worktree 曾核验为 clean；本报告归档后需再次运行 `git status` 确认 clean。GitHub/Gitee 的 `master` 已核对一致，验收分支不合并、不创建标签。
- 生产部署前仍需按 `docs/production-runbook.md` 建立真实数据库+附件备份，若采用 S3 则还需确认服务商对象恢复点。CI 的一次性恢复演练不等于已有生产数据备份；TLS 前置代理、维护窗口与值班负责人也属于部署时执行项。
- 已确认其他 P1/P2：无；但因 BLOCKER 停止，不能声称尚未复测的交互均无问题。豁免：无。

## 后续条件

在独立 `fix/v1.5.0-...` 分支修复通知业务跳转、通过独立终审并合入后，以新的 `master` 为基线重新执行 Release Acceptance。正式验收全部必需项通过且完成独立终审之前，不创建 `v1.5.0`。
