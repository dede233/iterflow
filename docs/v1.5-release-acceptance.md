# 迭程 IterFlow V1.5 正式发布最终重新验收

## 结论与版本边界

- 验收日期：2026-09-24（Asia/Shanghai）。**Release Verdict：PASS**；本报告仍需独立 Release Acceptance 终审，不代表已创建正式版本标签或已部署生产。
- 正式代码基线：`master` merge commit `a7e60e39893f162dd49b03d40f5ca975abd5d42d`，GitHub `origin/master` 与 Gitee `master` 已核对一致。
- 通知修复：`fdda6939c4d8e9e6211b4e6dfc31df85f1634b01`，仅修改通知前端页面及两份测试；经独立终审 PASS 后以 `--no-ff` 合并，无冲突。
- 旧稳定标签及回滚点：`v0.8.3` → `9bcdac1afe8499ca3c471c678b411eace60833a6`，未移动。**未创建 `v1.5.0`**。
- 本轮验收资产仅在 `release/v1.5.0-final-reacceptance`。从新 `master` 建立独立 worktree，复用旧分支测试提交 `ed93c2c80af615e687e5ee7716e00fee0a01d816`，新增真实通知发布链浏览器验收；旧 BLOCKED 报告未作为新结果复制。

## 必需门禁与证据

| 项目 | 结果 |
| --- | --- |
| Backend | 本轮验收分支在真实 PostgreSQL 环境运行 `pytest -q -W error`：**247 passed、1 skipped**；唯一跳过项是本机未提供 MinIO 的真实 S3 集成。专门的 `test_v1_e2e_postgres.py`：**4 passed、0 skipped**。合并后 `master` CI：245 passed、1 skipped；PostgreSQL 专项：7 passed、0 skipped。 |
| Frontend | `npm test`：**106 passed**；`npm run build`、`npm run check:api-types`、`npm run check:bundle` 均通过。最大 JavaScript chunk：**297.1 KiB**。 |
| Browser | 真实 Chromium：**7 passed**。先在一次性部署中运行首次登录/强制改密用例，再运行其余 6 项；涵盖个人中心、通知、真实发布链、登录和业务/管理页面。无 pageerror 或严重 console error。 |
| 真实通知发布链 | 一次性 PostgreSQL schema + 真实 Backend API + 实际前端浏览器：创建 MEMBER 提交人 → Feedback → ACCEPTED → Requirement → Version 关联及状态推进 → **POST** `/publish/check` → Publish；Release 为 SUCCESS，Feedback 为 ONLINE，通知含真实 Feedback ID。提交人点击“反馈 … 已上线”后，已读 POST 返回 200、浏览器进入 `/feedbacks/{id}`、详情加载成功、再次查询 `read_at != null`。 |
| 路由权限 | 在同一真实验收链中撤销提交人的角色后刷新身份，再点击已读通知；浏览器进入 `/forbidden`，通知本身未绕过 `rd.feedback.view`。单元测试还覆盖 Requirement、Version、Release、SYSTEM、未知类型、缺失 ID、已读再次点击和标记失败不跳转。 |
| 认证与权限 | 浏览器验证管理员与 MEMBER 登录、首次强制改密、常规改密、Profile、退出；后端测试覆盖 RBAC、SELF/ALL、数据范围、SUPER_ADMIN 最后一人保护和真实 PostgreSQL 并发。 |
| 主业务与事务 | 真实 PostgreSQL 验收测试覆盖反馈受理与转化、需求状态与版本关系、发布检查/发布、Release、通知、审计、revision 旧值 409、并发发布单一成功者，以及故障注入后的发布事务完全回滚。 |
| 文件与存储 | 全量后端测试覆盖业务附件权限隔离、standalone 文件 SELF/ALL、关联文件删除保护、上传 MIME/扩展名/签名/OOXML 校验、文件名净化及 Local Storage。CI 的 S3 作业完成真实 MinIO 往返；备份恢复作业完成数据库重建、附件恢复与 SHA256 比对。 |
| API 契约 | 合并后 CI 的 runtime/static OpenAPI parity：**4 passed**；本地生成 DTO 的 `check:api-types` 通过。发布检查按已确认的现有 **POST** 契约验收，旧清单中的 GET 为笔误。 |
| 数据库 | 一次性空 schema 迁移到 `0004_integrity`；`alembic check` 输出 `No new upgrade operations detected.`；本次无新增 migration，验收 schema 已删除。 |
| 质量与安全 | Ruff、format、Mypy、compileall 均通过；npm 完整与生产依赖审计均 **0 vulnerabilities**；pip-audit：**0 known vulnerabilities**。 |
| HTTP/响应式 | 隔离运行时 `/health`、`/ready` 均 **200**；Web 页面正常。浏览器在 **375、390、768、1280、1440 px** 验证登录、核心业务、个人中心与桌面管理页可见性及横向溢出。 |

合并后新 `master` 对应的 [GitHub Actions run 35969497303](https://github.com/dede233/iterflow/actions/runs/35969497303) 目标 SHA 正是 `a7e60e39893f162dd49b03d40f5ca975abd5d42d`，事件为 `push`。六项作业 `backend`、`frontend`、`docker_smoke`、`s3`、`browser_smoke`、`backup_restore` **全部 success**。Docker fresh-volume、生产构建、真实 S3 与备份恢复证据仅引用此新 SHA，不沿用旧 CI。

## 隔离、缺口与发布前事项

- 本地浏览器验收使用新 `master` 的前端开发代理连接一次性真实 API；生产 Web 构建与 Docker/Nginx 启动由同 SHA 的 CI 验证。本机未运行 Docker/MinIO，S3 本机用例被跳过，但该 SHA 的独立 `s3` CI job 已成功；这不是代码豁免。
- 一次性 PostgreSQL schema、Redis DB 15 和临时附件目录已清理；未修改默认开发数据库。原始 dirty `release/v1.5.0-acceptance` worktree 中的未提交报告/草稿保持原样；历史 `release/v1.5.0-reacceptance` 分支与其 BLOCKED 结论也保持原样。
- Git 状态：`master` worktree 与本轮 final acceptance worktree 均为 clean；原始 dirty 验收 worktree 仍保留其原有两处修改及一份未跟踪报告。
- 本轮确认的 BLOCKER：**无**。新增 P1/P2：**无**。Waiver：**无**。正式验收结论为 **PASS**，仍待独立终审。
- 实际生产部署前仍须依据 `docs/production-runbook.md` 核实生产数据库和附件备份/恢复点、TLS 前置代理、环境密钥、维护窗口、值班负责人及回滚演练。CI 一次性恢复演练不等于既有生产数据备份。
- 本验收资产分支不得合并回 `master`；仅提交并推送验收测试与本报告。独立终审通过且收到下一条明确指令前，不创建 `v1.5.0`。
